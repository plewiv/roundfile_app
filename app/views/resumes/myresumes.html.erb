<h1><%= @title %></h1>

<table>
  <tr>
    <th>Name</th>
    <th>User ID</th>
  </tr>

<% @resume.each do |resume| %>
  <tr>
    <td><%= link_to "#{resume.name}", "/viewresume/#{resume.id}" %></td>
    <td><%= resume.userid %></td>
  </tr>
<% end %>
</table>

