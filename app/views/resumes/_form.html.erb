<%= form_for(@resume) do |f| %>
  <% if @resume.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resume.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @resume.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	    <%= f.label "Resume Title" %><br /> 
		<%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Submit Title...", :disable_with => "Submitting..."  %>
  </div>
<% end %>